info:
  name: get overloaded
  type: http
  seq: 46

http:
  method: GET
  url: http://localhost:8080/sae/points/overloaded
  auth:
    type: basic
    username: paul
    password: "1234"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
