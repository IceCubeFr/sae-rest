info:
  name: get overloaded en user
  type: http
  seq: 47

http:
  method: GET
  url: http://localhost:8080/sae/points/overloaded
  auth:
    type: basic
    username: dupont
    password: "1234"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
