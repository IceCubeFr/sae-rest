info:
  name: get collectionPoint entier
  type: http
  seq: 11

http:
  method: GET
  url: http://localhost:8080/sae/points
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
