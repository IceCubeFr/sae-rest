info:
  name: post deposit avec erreur (wastetype n'existe pas)
  type: http
  seq: 25

http:
  method: POST
  url: http://localhost:8080/sae/deposits
  body:
    type: json
    data: |-
      {
        "userid": 2,
        "pointid": 10,
        "wastetypeid": 10000,
        "poids": 200
      }
  auth:
    type: basic
    username: dupont
    password: "1234"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
