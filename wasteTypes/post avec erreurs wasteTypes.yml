info:
  name: post avec erreurs wasteTypes
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/sae/wasteTypes
  body:
    type: json
    data: |-
      {
        "id":3,
        "nom": "plop",
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
