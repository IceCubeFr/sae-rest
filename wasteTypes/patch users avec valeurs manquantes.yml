info:
  name: patch users avec valeurs manquantes
  type: http
  seq: 44

http:
  method: PATCH
  url: http://localhost:8080/sae/users/3
  body:
    type: json
    data: |-
      {
        "id": 3,
        "login": "dupond",
        "role": "USER"
      }
  auth:
    type: basic
    username: paul
    password: "1234"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
