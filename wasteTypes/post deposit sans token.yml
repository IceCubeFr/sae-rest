info:
  name: post deposit sans token
  type: http
  seq: 27

http:
  method: POST
  url: http://localhost:8080/sae/deposits
  body:
    type: json
    data: |-
      {
        "userid": 2,
        "pointid": 10,
        "wastetypeid": 100,
        "poids": 200
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
