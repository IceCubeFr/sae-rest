info:
  name: patch collectionPoint sans token
  type: http
  seq: 21

http:
  method: PATCH
  url: http://localhost:8080/sae/points/1
  body:
    type: json
    data: |-
      {
        "id":1,
        "adresse":"Montpellier",
        "capaciteMax":700
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
